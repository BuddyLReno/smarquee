version: '2'
plugins:
  duplication:
    enabled: true
    checks:
      Similar code:
        enabled: false
    config:
      languages:
        - javascript
  eslint:
    enabled: true
    config:
      extensions:
        - .js
exclude_patterns:
  - 'tests/'
  - 'demo/'
  - 'dist/'
